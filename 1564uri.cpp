#include<bits/stdc++.h>
using namespace std;

int main (){
    double a, b, c, media;
    cout << fixed;
    cout.precision(1);
    cin >> a >> b >> c;
    media = ((a * 0.2) + (b * 0.3) + (c * 0.5));
    cout << "MEDIA = " << media << endl;

    return 0;
}